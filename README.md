This is a chess engine that calculates moves using a minimax algorithm with alpha-beta pruning. 

The minimax algorithm evaluates a state with a heuristic that has been arrived at through genetic programming: the program creates heuristic functions and puts them through a "tournament" where they are ordered by their success; at random these successful heuristics are "cross bred," much like DNA, or are randomly mutated.
